#import "app.h"
#import "app_nav.h"

route customize_nav {
    title "Customize Navigation"
    group "App"
    page customize_nav
}

customize_nav :: (app: InbeApp*) #ui {
    Screen root: {
    app_draw_customize_nav_page(app)
    }
}
